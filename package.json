{
  "name": "attestor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Attestor — the notary and forensics lab for the agent economy. An A2MCP service on OKX.AI that compiles vague task specs into verifiable acceptance criteria, inspects delivered work against them with evidence-backed harnesses, and anchors signed reports on X Layer.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
